import 'package:flutter_test/flutter_test.dart';
import 'package:questrip/widget/intro/intro.dart';

void main() {
  testWidgets('IntroWidget Test', (WidgetTester tester) async {
    await tester.pumpWidget(IntroWidget());
    // Begin
  });
}
